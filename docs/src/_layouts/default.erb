<!doctype html>
<html lang="en">
  <head>
    <%= render "head", metadata: site.metadata, title: page[:title] %>
  </head>
  <body class='<%= "#{page[:layout]} #{page[:page_class]}" %>' data-controller="search">
    <%= render "top_nav", metadata: site.metadata, page: page %>

    <%= yield %>

    <%= render "footer", metadata: site.metadata %>
  </body>
</html>
